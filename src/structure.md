# Структура работы для текущей ML-only версии

## 1. Анализ предметной области и существующих подходов

1.1. Особенности эксплуатации ГРС/ПРГ.

1.2. Газовый фильтр как объект диагностирования.

1.3. Процесс технического обслуживания фильтров.

AS-IS:

```text
датчики -> SCADA -> пороги -> оператор -> ППР/аварийное ТО
```

TO-BE:

```text
датчики/симулятор -> признаки -> ML-прогноз RUL -> правила -> рекомендация ТО
```

1.4. Нормативная и регламентная основа обслуживания.

1.5. Входные параметры оценки состояния фильтра: `P_in`, `P_out`, `deltaP`, `Q`, `T`, timestamp, история ТО, паспортные данные.

1.6. Анализ методов предиктивного обслуживания: пороговые методы, статистические методы, физико-математические модели, классическое ML, гибридные методы, explainable AI.

1.7. Ограничения существующих подходов: шум и пропуски данных, влияние расхода на перепад давления, редкость отказов, невозможность прямого наблюдения уровня засорения, недостаточная интерпретируемость чисто ML-подходов.

1.8. Постановка задачи исследования: разработать симулятор, feature builder, ML baseline, аналитический RUL, rule-based слой и объяснимую рекомендацию по ТО.

## 2. Математическая и информационная модель системы

2.1. Концептуальная модель:

```text
газовый фильтр -> датчики -> телеметрия -> признаки -> ML-прогноз -> правила -> рекомендация ТО
```

2.2. Информационная модель входных и выходных данных:

- входные параметры симулятора;
- наблюдаемые телеметрические параметры;
- расчетные диагностические признаки;
- скрытые переменные симулятора;
- целевые переменные для обучения;
- выходные параметры системы.

2.3. Модель деградации газового фильтра.

2.4. Модель телеметрического снимка.

2.5. Модель перепада давления с учетом расхода, плотности, температуры и засорения.

2.6. Модель состояний фильтра: `normal`, `warning`, `critical`, `unknown`.

2.7. Модель остаточного ресурса: `RUL_oracle_h`, `RUL_analytic_h`, `RUL_ml_h`, `RUL_fused_h`.

2.8. Модель доверия к прогнозу: `confidence_data`, `confidence_model`, `confidence_consistency`, `confidence_total`.

2.9. Формализация продукционных правил.

2.10. Обобщенная гибридная модель:

```text
ML-прогноз + аналитический RUL + качество данных + confidence + правила -> решение
```

## 3. Разработка алгоритмов и методик

3.1. Методика генерации синтетических данных.

3.2. Сценарии моделирования эксплуатации.

3.3. Контроль качества данных.

3.4. Подготовка признаков.

3.5. Rule-based диагностика.

3.6. ML-прогнозирование RUL и состояния фильтра.

3.7. Оценка доверия к прогнозу.

3.8. Гибридное принятие решений.

3.9. Методика интерпретации результатов.

3.10. Методика оценки эффективности: MAE, RMSE, F1-score, ложные тревоги, пропущенные критические состояния, время раннего предупреждения, сравнение rule-based baseline, аналитического RUL, ML baseline и гибридного решения.

## 4. Программная реализация и экспериментальная проверка

4.1. Требования к программному продукту.

4.2. Архитектура программного продукта:

- Simulator;
- Feature Builder;
- ML Baseline;
- Analytic RUL;
- Rule Engine;
- Decision Service;
- Visualization;
- Audit Log.

4.3. Реализация симулятора.

4.4. Реализация подготовки данных и признаков.

4.5. Реализация ML baseline.

4.6. Реализация rule-based модуля.

4.7. Реализация гибридного контура.

4.8. Реализация визуализации и карточек объяснения.

4.9. Экспериментальная проверка на сценариях симулятора.

4.10. Сравнение подходов:

- пороговые правила;
- аналитический RUL;
- ML baseline;
- ML + правила.

4.11. Анализ результатов и ограничений.

## Заключение

Итоги работы, выполненные задачи, основные результаты, ограничения и направления развития.

## Приложения

- Таблица параметров симулятора.
- Словарь выходного датасета.
- Конфигурации сценариев моделирования.
- База продукционных правил.
- Диаграмма компонентов.
- Примеры графиков временных рядов.
- Примеры explanation-card.
- Фрагменты программного кода.
